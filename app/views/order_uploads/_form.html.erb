<%= form_with(model: order_upload) do |form| %>
  <% if order_upload.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(order_upload.errors.count, "error") %> prohibited this order_upload from being saved:</h2>

      <ul>
        <% order_upload.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :csv_file, style: "display: block" %>
    <%= form.file_field :csv_file %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
